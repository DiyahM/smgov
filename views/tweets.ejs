<!DOCTYPE html>
<html lang="en">
  <head>
  	<!-- styles -->
    <link href="/css/bootstrap.css" rel="stylesheet">
    <link href="/css/bootstrap-responsive.min.css" rel="stylesheet">
  </head>
  <body>
	<h3 class="capitalize"><%= title %></h3>
    <ul>
		<% var len = tweets.length - 1%>
		<% for (var i=0; i<len; i++) { %>
			<% var tweet = JSON.parse(tweets[i]) %>
			<li><%= tweet.user.screen_name %>: <%= tweet.text %></li>
		<% } %>
    </ul>
  </body>
</html>