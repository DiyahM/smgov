<!DOCTYPE html>
<html lang="en">
  <head>
  	<!-- styles -->
    <link href="/css/bootstrap.css" rel="stylesheet">
    <link href="/css/bootstrap-responsive.min.css" rel="stylesheet">
  </head>
  <body>
    <h3 class="capitalize"><%= title %></h3>
  
  
    <ul class="thumbnails">
	  		<% var len = posts.length %>
			<% for (var i=0; i<len; i++) { %>
				<% var post = JSON.parse(posts[i]) %>
				<li>
					<div class="thumbnail"><a href='<%= post.link %>' target="_blank"><img src='<%= post.picture %>'/></a>
						
					</div>
				</li>
			<% } %>
	</ul>
  

</body>
</html>
